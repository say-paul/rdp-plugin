[project]
name = "caryam-robot-arm-library"
version = "0.1.0"
description = "Blender add-on for discovering and placing Robot Library"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[project.optional-dependencies]
mujoco = ["mujoco>=3.2"]